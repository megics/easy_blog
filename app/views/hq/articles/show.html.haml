- content_for :toolbar do
  = link_to edit_hq_article_path(@article), class: 'btn btn-default' do
    %i.icon-pencil
    = "Düzenle"
.panel.panel-default
  .panel-heading
    %i.icon-edit.icon-large
    Makale Detay
  .panel-body
    = show_for @article do |s|
      = s.attribute :title
      = s.attribute :text
      = s.attribute :created_at
      = s.attribute :updated_at